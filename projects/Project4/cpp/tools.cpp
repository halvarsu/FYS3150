#include <random>
#include "tools.h"

void readData(string filename, int& NMC, double & T, int &L, bool parallel, bool timeit, bool save_to_file){
    return;
}

int periodic(int i, int limit, int add)
{
    return (i + limit + add) % (limit);
}

